#include "minishell.h"

void	pwd_(void)
{
	// int		exit_status;
	// char	*path;

	printf("%s\n", g_mshell->pwd);
	// if ()
	// exit_status = 0;
	// path = getcwd(NULL, 0);
	// if (path == NULL)
	// 	exit_status = 1;
	// if (path != NULL)
	// 	printf("%s\n", path);
	// add_exit_status_to_env(exit_status);
	// free(path);
}
